pipeline {
  agent {
    node {
      label 'docker'
    }

  }
  stages {
    stage('Run build') {
      steps {
        configFileProvider([configFile(fileId: 'lineageos-amami-env', targetLocation: './.env')]) {}
        sh 'mkdir -p ./zips'
        sh 'mkdir -p ./logs'
        sh 'docker-compose -f docker-compose.amami.yml up --force-recreate --renew-anon-volumes --abort-on-container-exit --exit-code-from lineageos-build-amami'
      }
    }

    stage('Archive build artifacts') {
      steps {
        archiveArtifacts 'zips/amami/**, logs/**'
      }
    }

    stage('Clean up') {
      steps {
        sh 'docker-compose -f docker-compose.amami.yml down --rmi local'
      }
    }

  }
}
